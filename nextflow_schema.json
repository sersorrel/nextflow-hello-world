{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/sersorrel/nextflow-hello-world/main/nextflow_schema.json",
    "title": "nextflow-hello-world pipeline parameters",
    "description": "hello, nextflow!",
    "type": "object",
    "definitions": {
        "parameters": {
            "title": "Parameters",
            "type": "object",
            "description": "things you can customise",
            "default": "",
            "properties": {
                "to": {
                    "type": "string",
                    "default": "world",
                    "fa_icon": "fas fa-user",
                    "description": "Who to greet"
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/definitions/parameters"
        }
    ]
}
